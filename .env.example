# This is a TEMPLATE file showing what your .env should look like.
# To use it:
#   1. Copy this file and rename the copy to just ".env"
#   2. Fill in the real values
#   3. NEVER commit .env to GitHub — it contains passwords!

APP_ENV=development
APP_DEBUG=true

DB_HOST=localhost
DB_PORT=5432
DB_NAME=churn_db
DB_USER=your_username
DB_PASSWORD=your_password_here

MODEL_VERSION=v1
```

**Save it.** ✅

---

